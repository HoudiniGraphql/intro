/// <references types="houdini-react">
/** @type {import('houdini').ConfigFile} */
const config = {
	plugins: {
		"houdini-react": {},
	},
	types: {
		SpeciesMove: {
			keys: ["name"],
		},
	},
};

export default config;
